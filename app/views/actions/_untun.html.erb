<%= link_to tun_path(current_user.tuns.find_by(post_id: post.id)), id: "js-tun-button-for-post-#{post.id}", class:"float-right", data: { turbo_method: :delete }, remote: true  do %>
  <%= icon('fas', 'star') %>
  ツン<%= post.tuns.count %>
<% end %>