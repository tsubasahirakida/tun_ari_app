<%= turbo_frame_tag dom_id(post, :dere_post) do %>
    <%= link_to deres_path(post_id: post.id),
              id: "js-dere-button-for-post-#{post.id}",
              data: { turbo_method: :post } do %>
    <%= icon('far', 'star') %>
    デレ<%= post.deres.count %>コ
  <% end %>
<% end %>