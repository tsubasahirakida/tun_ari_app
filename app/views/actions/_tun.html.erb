<%= turbo_frame_tag dom_id(post, :tun_post) do %>
    <%= link_to tuns_path(post_id: post.id),
              id: "js-tun-button-for-post-#{post.id}",
              data: { turbo_method: :post } do %>
    <i class="fa-regular fa-star"></i>
    ツン<%= post.tuns.count %>コ
  <% end %>
<% end %>