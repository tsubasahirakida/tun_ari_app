<%= turbo_frame_tag dom_id(post, :tun_post) do %>
    <%= link_to "ツン#{post.tuns.count}コ",
        tuns_path(post_id: post.id),
        id: "js-tun-button-for-post-#{post.id}",
        class: "btn btn-outline btn-info opacity-50",
        data: { turbo_method: :post } %>
<% end %>