<div class="border border-gray-200 p-6 rounded-lg mt-1">
  <h2 class="text-lg text-gray-900 font-medium title-font mb-2"><%= template.title %></h2>
  <p class="leading-relaxed text-base"><%= template.body %></p>
</div>
<%= button_to "これにする", new_post_path, { method: :get, params: {tempalate_body: "#{template.body}", character_id: "#{post.character_id}"}, class: "mt-1 btn btn-outline btn-secondary" } %>