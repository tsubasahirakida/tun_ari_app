<div class="card border mt-5 card-bordered border-gray-200">
  <div class="overflow-hidden">
    <div class="items-center text-center m-10 my-3">
      <h2 class="text-lg text-gray-900 font-medium title-font mb-2"><%= template.title %></h2>
      <p class="leading-relaxed text-base"><%= template.body %></p>
      <%= button_to "これにする", edit_post_path(post), { method: :get, params: {tempalate_body: "#{template.body}" }, class: "mx-10 mt-3 mb-1 btn btn-outline btn-secondary" } %>
    </div>
  </div>
</div>